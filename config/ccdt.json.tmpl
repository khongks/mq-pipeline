{
    "channel":
    [
        {
            "name": "${channel}",
            "clientConnection":
            {
                "connection":
                [
                    {
                        "host": "${hostname}",
                        "port": 443
                    }
                ],
                "queueManager": "${qmgr_name}"
            },
            "transmissionSecurity":
            {
              "cipherSpecification": "TLS_RSA_WITH_AES_256_CBC_SHA256",
              "certificateLabel": "${cert_name}"
            },
            "type": "clientConnection"
        }
   ]
}